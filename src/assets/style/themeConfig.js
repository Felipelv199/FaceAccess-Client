import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#404b69',
    },
    secondary: {
      main: '#f73859',
    },
    background: {
      default: '#283149',
    },
    text: {
      main: '#dbedf3',
    },
  },
});

export default theme;
